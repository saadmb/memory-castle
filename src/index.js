import React from 'react';
import {
  Text,
  View
} from 'react-native'

import {Landing} from 'modules'

export default class Main extends React.Component {
  render() {
    return (
        <Landing />
    )
  }
}
